extends ../layout

block content
  .term-outer
    .progress-bar-wrapper
      .progress-bar

    // ================= CONTENT =================
    .container
      .header-inner
        h1.animate-text Terms & Conditions

      .terms-container
        // TOC
        .toc
          h2.animate-text Table of Contents
          ul
            li.animate-text section's name
            li.animate-text section's name
            li.animate-text section's name
            li.animate-text section's name
            li.animate-text section's name
            li.animate-text section's name

        // TERMS
        .term-section
          h2.animate-text Overview
          p.animate-text This website is operated by JK Supply LLC. Throughout the site, the terms “we”, “us” and “our” refer to JK Supply LLC.
          p.animate-text By visiting our site and/or purchasing something from us, you engage in our “Service” and agree to be bound by the following terms and conditions.
          p.animate-text Please read these Terms of Service carefully before accessing or using our website.
          p.animate-text Any new features or tools which are added to the current store shall also be subject to the Terms of Service.
          p.animate-text Our store is hosted on Shopify Inc.

          h2.animate-text General Conditions
          p.animate-text We reserve the right to refuse service to anyone for any reason at any time.
          p.animate-text You understand that your content may be transferred unencrypted.

          h2.animate-text Accuracy, completeness and timeliness of information
          p.animate-text We are not responsible if information made available on this site is not accurate.

          h2.animate-text Modifications to the service and prices
          p.animate-text Prices for our products are subject to change without notice.
          p.animate-text We reserve the right at any time to modify or discontinue the Service.
          p.animate-text We shall not be liable for any modification or discontinuance.

          h2.animate-text Products or services (if applicable)
          p.animate-text Certain products or services may be available exclusively online.
          p.animate-text We have made every effort to display accurately the colors of our products.
          p.animate-text We reserve the right to limit sales or discontinue products.
          p.animate-text We do not warrant that the quality of products will meet expectations.


    // ================= SCRIPTS =================
    script(src="https://cdn.jsdelivr.net/npm/gsap@3/dist/gsap.min.js")
    script(src="https://cdn.jsdelivr.net/npm/gsap@3/dist/ScrollTrigger.min.js")

    script.
      document.addEventListener("DOMContentLoaded", function () {
        if (!window.gsap || !window.ScrollTrigger) return;

        gsap.registerPlugin(ScrollTrigger);

        // ================= PROGRESS BAR =================
        gsap.to(".progress-bar", {
          scaleX: 1,
          ease: "none",
          scrollTrigger: {
            start: 0,
            end: () => document.documentElement.scrollHeight - window.innerHeight,
            scrub: true
          }
        });

        // ================= PROGRESS BAR TOP SCROLL =================
        let lastScroll = window.pageYOffset || document.documentElement.scrollTop;
        const progressWrapper = document.querySelector(".progress-bar-wrapper");

        window.addEventListener("scroll", () => {
          const currentScroll = window.pageYOffset || document.documentElement.scrollTop;
          progressWrapper.style.top = currentScroll < lastScroll ? "75px" : "0px";
          lastScroll = currentScroll <= 0 ? 0 : currentScroll;
        });

        // ================= TEXT ANIMATION =================
        gsap.utils.toArray(".animate-text").forEach((elem, i) => {
          gsap.from(elem, {
            opacity: 0,
            y: 50,
            scale: 0.95,
            duration: 0.8,
            ease: "power3.out",
            scrollTrigger: {
              trigger: elem,
              start: "top 85%",
              toggleActions: "play none none none"
            },
            delay: i * 0.05
          });
        });
      });