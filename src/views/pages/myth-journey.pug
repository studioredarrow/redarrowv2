extends ../layout

block content
  .myth-wrapper
    .myth-header
      span.breadcrumb > Myth Journey
      h4.status Pretending to think very hard 🤖...

    .myth-card
      img(
        src="/images/loding-banner-image.png"
        alt="Thinking illustration"
      )


    .chat-input
      input(
        type="text"
        placeholder="Type anything..."
      )
