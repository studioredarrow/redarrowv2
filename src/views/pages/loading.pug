extends ../layout

block content
  .loader-wrapper
    .arrow
      img(src="/images/loading-page-arrow.png", alt="arrow")
    .loader-text MYTH CREATION

    style.
            body{
              background-color : red !important;
            }
            .logo-container {
              background-color: transparent !important;
            }
            .logo-container.fixed {
              background-color: transparent !important;
            }


  //- script.
  //-   setTimeout(() => {
  //-     window.location.replace("#{targetUrl}");
  //-   }, 500); 
    

